{
  "name": "login",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd Server && nodemon server",
    "client": "cd Client && npm start",
    "dev": "set PORT = 8000 && set HOST = 'http://localhost:3000' && concurrently --kill-others \"npm run server\" \" npm run client\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
